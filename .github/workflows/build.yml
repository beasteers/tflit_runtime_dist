on: push
name: Compile
jobs:
  build:
    name: Build
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        python: [3.5, 3.6, 3.7, 3.8]
        os: [macos-latest, windows-latest, ubuntu-latest]

    steps:
    - uses: actions/setup-python@v1
      with:
        python-version: ${{ matrix.python }}

    - name: Clone
      run: git clone https://github.com/tensorflow/tensorflow.git
    - name: Installs
      run: |
        sudo apt-get install swig libjpeg-dev zlib1g-dev -y
        python -m pip install --upgrade pip
        pip install numpy pybind11
    - name: Build
      run: |
        sh tensorflow/tensorflow/lite/tools/make/download_dependencies.sh
        sh tensorflow/tensorflow/lite/tools/pip_package/build_pip_package.sh
        rm -r tensorflow
    - name: show
      run: ls * */*
    # - name: Publish package to TestPyPI
    #   uses: pypa/gh-action-pypi-publish@master
    #   with:
    #     user: ${{ secrets.pypi_user }}
    #     password: ${{ secrets.pypi_password }}
    #     repository_url: testpypi #https://test.pypi.org/legacy/
    #     skip_existing: true
